import { StyleSheet } from "react-native";

export default StyleSheet.create({
  block: {
    height: 150,
    width: "100%",
    marginBottom: 2,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around",
  },
});
